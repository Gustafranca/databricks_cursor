resources:
  dashboards:
    bakehouse_performance:
      display_name: Bakehouse Executive Performance
      file_path: ../src/dashboards/bakehouse.lvdash.json
      warehouse_id: ${var.warehouse_id}
      dataset_catalog: ${var.catalog}
      dataset_schema: ${resources.schemas.bakehouse_gold.name}
